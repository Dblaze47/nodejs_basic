extends ../layout

block content
    form(action="/lead/" + lead.id + "/edit", method="POST")
        input(type="email", name="lead_email", placeholder="enter you email address", value=lead.email)
        |
        |
        button(type="submit") Save
